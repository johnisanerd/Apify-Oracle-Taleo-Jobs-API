[project]
name = "apify-oracle-taleo-jobs-api"
version = "0.1.0"
description = "Example client for the Oracle Fusion Recruiting and Taleo Jobs API Apify Actor"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apify-client>=3,<4",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
